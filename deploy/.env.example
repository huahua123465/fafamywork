# 复制成 .env 再填。.env 不会提交到 git。
# 管理密码：登录 /admin 用的，改完执行 docker compose up -d api 生效
ADMIN_PASSWORD=换成你自己的密码
# 签发登录凭证用的密钥，随便一串随机字符即可：
#   head -c 32 /dev/urandom | base64 | tr -d '/+='
TOKEN_SECRET=换成一串随机字符
